# Cached assets service
 Endpoints:
 * GET /api/assetmap         (Get text details of asset) 
 * GET /api/asset            (Get asset from an api call. Cache it in Redis to avoid api calls in future) 
 * /alive
 * /ready
```
